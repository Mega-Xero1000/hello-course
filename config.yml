title: Hello
description: Este curso es para a GitHub.prender más sobre
template:
  repo: hello-template
  name: hello
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
